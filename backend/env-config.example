# Example env configuration. Do NOT commit real secrets.

MONGO_URI=
JWT_SECRET=
PORT=5000
NODE_ENV=production

# Email (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Frontend base for frontend build-time
VITE_API_URL=

# Admin contact
ADMIN_EMAIL=
ADMIN_PHONE=

# Twilio (use Messaging Service SID in production if possible)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_SENDER_NAME=

